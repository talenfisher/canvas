{
  "name": "@talenfisher/canvas",
  "version": "0.3.2",
  "description": "A lightweight canvas library",
  "main": "dist/index.js",
  "scripts": {
    "test": "http-server -c-2 -o --cors",
    "build": "tsc --build",
    "watch": "tsc --watch",
    "postversion": "git push --follow-tags"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jest-canvas-mock": "^1.1.0",
    "typescript": "^3.2.2"
  }
}
