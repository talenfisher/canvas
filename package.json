{
  "name": "@talenfisher/canvas",
  "version": "0.2.1",
  "description": "A lightweight canvas library",
  "main": "dist/index.js",
  "scripts": {
    "test": "http-server -c-2 -o",
    "build": "tsc --build",
    "watch": "tsc --watch",
    "postversion": "git push --follow-tags"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jest-canvas-mock": "^1.1.0",
    "typescript": "^3.2.2"
  }
}
